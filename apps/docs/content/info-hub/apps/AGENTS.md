# Apps Info Hub Agent Directives

> **Scope**: `content/info-hub/apps/` (Documentation for monorepo applications).

## Monorepo Apps Catalog
- `companion`: Next.js web assistant & prayer tracker (port `3012`).
- `companion-expo`: React Native / Expo mobile app for iOS & Android.
- `docs`: Next.js 16 documentation & planning hub (port `4003`).
- `home`: Vercel proxy landing gateway (port `4000`).
- `planner`: Kanban board & task manager (port `4002`).
- `tv-set`: Interactive retro TV & p5.js portfolio app (port `3011`).

## Instructions for Documentation Updates
- Always include port numbers, environment flags, and run commands (`yarn dev --filter=<app>`).
- Highlight routing strategies (App Router, Expo Router, Vercel rewrites).
- Document component structures and primary state stores.
