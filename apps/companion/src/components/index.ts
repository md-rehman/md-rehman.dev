// expo modules (EASY API for writing native modules) -> Bato youtube channel
// native modules
// turbo modules (React Native)
// Re-animated
// Gesture Handler (React Native Gesture Handler)
// Work let ()
// RN new Arch Bridge (JSI introduced)
// List (Flash List & Legend List) Performace
// Navigation (EXPO Router (under the hood uses React Navigation) & React Navigation)
// Expo Repo (check component like Expo image, )

// TRPC (when not using supabase)
